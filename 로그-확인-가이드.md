# 로그 확인 가이드

## 📋 목차
1. [백엔드 로그](#백엔드-로그)
2. [프론트엔드 로그](#프론트엔드-로그)
3. [데이터베이스 로그](#데이터베이스-로그)

---

## 🔧 백엔드 로그

### 1. 실시간 로그 확인
백엔드 서버를 실행한 **터미널**에서 확인할 수 있습니다:

```bash
cd /Users/baesh/Desktop/BAESH-Career/baesh-career-backend
npm run dev
```

터미널에 다음과 같은 로그가 표시됩니다:
- ✅ 서버 시작 메시지: `🚀 Server running on http://localhost:3001`
- ✅ API 요청 로그: 각 API 엔드포인트 호출 시
- ❌ 에러 로그: `console.error()`로 출력되는 에러 메시지

### 2. 주요 로그 위치

#### 서버 시작 로그
```
🚀 Server running on http://localhost:3001
📝 Environment: development
```

#### 인증 관련 로그
- **회원가입 에러**: `src/controllers/authController.ts:66`
- **로그인 에러**: `src/controllers/authController.ts:112`

#### 사용자 프로필 로그
- **프로필 조회 에러**: `src/controllers/userController.ts:104`
- **프로필 업데이트 에러**: `src/controllers/userController.ts:169`

#### AI 서비스 로그
- **AI API 에러**: `src/controllers/aiController.ts:30`
- **AI 스트림 에러**: `src/controllers/aiController.ts:67`

#### 게시물 로그
- **게시물 조회 에러**: `src/controllers/postController.ts:60`
- **게시물 생성 에러**: `src/controllers/postController.ts:108`
- **좋아요 에러**: `src/controllers/postController.ts:133`

#### 지원 관련 로그
- **지원 목록 조회 에러**: `src/controllers/applicationController.ts:23`
- **지원 생성 에러**: `src/controllers/applicationController.ts:51`

### 3. 로그 파일로 저장하기 (선택사항)

터미널에서 로그를 파일로 저장하려면:

```bash
cd /Users/baesh/Desktop/BAESH-Career/baesh-career-backend
npm run dev > backend.log 2>&1
```

로그 파일 확인:
```bash
tail -f backend.log  # 실시간 확인
cat backend.log      # 전체 로그 확인
```

---

## 🌐 프론트엔드 로그

### 1. 브라우저 개발자 도구

**Chrome/Edge:**
- `F12` 또는 `Cmd + Option + I` (Mac) / `Ctrl + Shift + I` (Windows)
- 또는 우클릭 → "검사"

**Safari:**
- `Cmd + Option + I`
- 먼저 Safari 설정 → 고급 → "메뉴 막대에서 개발자용 메뉴 보기" 활성화 필요

### 2. Console 탭

브라우저 개발자 도구의 **Console** 탭에서 확인:
- ✅ `console.log()` 메시지
- ⚠️ `console.warn()` 경고
- ❌ `console.error()` 에러
- 🔍 네트워크 요청 실패 메시지

### 3. Network 탭

**Network** 탭에서 확인:
- API 요청/응답 상태
- 요청 URL, 메서드, 상태 코드
- 요청/응답 데이터 (Preview 탭)

### 4. 주요 프론트엔드 로그 위치

#### 프로필 관련
- 프로필 로드 실패: `src/pages/Profile.tsx`
- 프로필 로드 실패: `src/pages/CloneHub.tsx`

#### 네트워킹 관련
- 게시물 로드 실패: `src/pages/Networking.tsx`

#### API 호출 관련
- 모든 API 호출: `src/utils/api.ts`
- 에러 처리: 각 서비스 파일 (`src/services/*.ts`)

---

## 🗄️ 데이터베이스 로그

### 1. Prisma Studio로 데이터 확인

```bash
cd /Users/baesh/Desktop/BAESH-Career/baesh-career-backend
npm run prisma:studio
```

브라우저에서 `http://localhost:5555` 접속하여:
- 데이터베이스의 모든 테이블 확인
- 데이터 조회/수정/삭제
- 관계 확인

### 2. SQLite 데이터베이스 파일

개발 환경에서는 SQLite를 사용하므로:

```bash
cd /Users/baesh/Desktop/BAESH-Career/baesh-career-backend
ls -la prisma/dev.db  # 데이터베이스 파일 위치
```

SQLite CLI로 직접 확인:
```bash
sqlite3 prisma/dev.db
.tables              # 모든 테이블 목록
SELECT * FROM User;  # 사용자 데이터 확인
```

---

## 🔍 로그 레벨별 확인 방법

### 일반 정보 (INFO)
- 백엔드: 터미널 출력
- 프론트엔드: 브라우저 Console

### 경고 (WARN)
- 백엔드: 터미널에 노란색으로 표시
- 프론트엔드: 브라우저 Console (노란색 아이콘)

### 에러 (ERROR)
- 백엔드: 터미널에 빨간색으로 표시 (`console.error()`)
- 프론트엔드: 브라우저 Console (빨간색 아이콘)

---

## 💡 유용한 팁

### 1. 실시간 로그 모니터링
```bash
# 백엔드 로그 실시간 확인
cd /Users/baesh/Desktop/BAESH-Career/baesh-career-backend
npm run dev | grep -E "error|Error|ERROR"
```

### 2. 특정 에러만 필터링
브라우저 Console에서:
- 필터 아이콘 클릭
- "Errors" 또는 "Warnings" 선택
- 검색창에 키워드 입력

### 3. 로그 레벨 설정 (향후 개선)
프로덕션 환경에서는 `winston` 같은 로깅 라이브러리 사용 권장

---

## 📝 로그 예시

### 백엔드 성공 로그
```
🚀 Server running on http://localhost:3001
📝 Environment: development
POST /api/auth/login 200
GET /api/users/profile 200
```

### 백엔드 에러 로그
```
Signup error: Error: Email already exists
Login error: Error: Invalid credentials
Get profile error: Error: User not found
```

### 프론트엔드 로그
```
프로필 로드 실패: Error: Failed to fetch
게시물 로드 실패: Error: Network request failed
```

---

## 🚀 빠른 참조

| 로그 종류 | 확인 위치 | 명령어/방법 |
|---------|---------|-----------|
| 백엔드 서버 로그 | 터미널 | `npm run dev` |
| 프론트엔드 로그 | 브라우저 Console | `F12` → Console 탭 |
| API 요청/응답 | 브라우저 Network | `F12` → Network 탭 |
| 데이터베이스 | Prisma Studio | `npm run prisma:studio` |

