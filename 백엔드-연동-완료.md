# 백엔드 연동 완료 상태

## ✅ 완료된 기능

### 1. 인증 시스템
- ✅ **로그인**: 백엔드 API 연동 완료 (`/api/auth/login`)
- ✅ **회원가입**: 백엔드 API 연동 완료 (`/api/auth/signup`)
- ✅ **JWT 토큰**: 로컬 스토리지에 저장, 모든 API 요청에 자동 포함
- ✅ **사용자 정보**: 로그인 시 사용자 정보 저장

### 2. 프로필 관리
- ✅ **프로필 조회**: 백엔드에서 사용자 프로필 가져오기 (`/api/users/profile`)
- ✅ **프로필 수정**: 백엔드에 프로필 저장 (`/api/users/profile` PUT)
- ✅ **프로필 데이터**: 모든 프로필 정보가 Prisma DB에 저장됨

### 3. 채팅 세션 (AI 클론)
- ✅ **세션 목록**: 백엔드에서 모든 세션 가져오기 (`/api/sessions`)
- ✅ **세션 생성**: 새 대화 세션 생성 (`/api/sessions` POST)
- ✅ **세션 저장**: 대화 내용 백엔드에 저장 (`/api/sessions/:id` PUT)
- ✅ **세션 삭제**: 세션 삭제 (`/api/sessions/:id` DELETE)
- ✅ **대화 로그**: 모든 대화 내용이 DB에 저장됨

### 4. AI 서비스
- ✅ **AI 채팅**: 백엔드 프록시를 통해 Upstage API 호출 (`/api/ai/chat`)
- ✅ **스트리밍 채팅**: SSE를 통한 실시간 응답 (`/api/ai/chat/stream`)
- ✅ **API 키 보안**: 프론트엔드에서 직접 호출하지 않고 백엔드에서 처리

### 5. 네트워킹 페이지
- ✅ **게시물 조회**: 백엔드에서 게시물 목록 가져오기 (`/api/posts?type=networking`)
- ✅ **게시물 작성**: 새 게시물 백엔드에 저장 (`/api/posts` POST)
- ✅ **좋아요**: 게시물 좋아요 기능 (`/api/posts/:id/like` POST)
- ✅ **데이터 저장**: 모든 게시물이 DB에 저장됨

### 6. 라운지 페이지 (채용/활동)
- ✅ **지원 기능**: 채용 공고 지원 시 백엔드에 저장 (`/api/applications` POST)
- ✅ **지원 내역**: 지원한 공고/활동이 백엔드에 저장됨
- ⚠️ **활동 지원**: 현재는 alert만 표시 (백엔드 API는 준비됨)

## 📊 데이터 저장 위치

모든 데이터는 **백엔드 SQLite 데이터베이스**에 저장됩니다:

1. **사용자 정보** (`User` 테이블)
   - 이메일, 비밀번호(해시), 이름
   - 로그인 시 자동 저장

2. **프로필 정보** (`UserProfile` 테이블)
   - 학교, 전공, 상태, 스킬, 관심사, 목표
   - 자격증, 수상, 경력, 포트폴리오, 조직

3. **채팅 세션** (`ChatSession` 테이블)
   - 세션 제목, 모드, 메시지 히스토리
   - 모든 대화 내용이 JSON으로 저장

4. **게시물** (`Post` 테이블)
   - 제목, 내용, 태그, 좋아요 수
   - 네트워킹 게시물이 모두 저장됨

5. **지원 내역** (현재 임시 저장)
   - 채용 공고 지원 정보
   - 지원 동기, 지원 시간

## 🔄 작동 흐름

### 로그인 시:
1. 사용자가 로그인 → 백엔드 `/api/auth/login` 호출
2. 백엔드가 JWT 토큰 발급 → 프론트엔드에 저장
3. 이후 모든 API 요청에 토큰 자동 포함

### 채팅 시:
1. 사용자가 메시지 입력 → 프론트엔드에서 세션에 추가
2. AI 응답 받기 → 백엔드 `/api/ai/chat/stream` 호출
3. 응답 완료 후 → 세션 전체를 백엔드에 저장 (`/api/sessions/:id` PUT)
4. **모든 대화 내용이 DB에 저장됨**

### 게시물 작성 시:
1. 사용자가 게시물 작성 → 백엔드 `/api/posts` POST 호출
2. 백엔드가 DB에 저장 → 프론트엔드에 새 게시물 반환
3. **게시물이 영구 저장됨**

### 지원 시:
1. 사용자가 지원하기 클릭 → 백엔드 `/api/applications` POST 호출
2. 백엔드가 지원 내역 저장 → 지원 현황 페이지에서 확인 가능
3. **지원 정보가 DB에 저장됨**

## ⚠️ 주의사항

1. **OAuth 로그인**: 현재는 더미 토큰 사용 (실제 구현 필요)
2. **활동 지원**: UI는 있지만 백엔드 연동 필요
3. **지원 내역 조회**: MyApplications 페이지는 더미 데이터 사용 (백엔드 연동 필요)

## 🚀 다음 단계

1. **Application 모델 추가**: Prisma 스키마에 Application 모델 추가
2. **MyApplications 연동**: 지원 내역 조회 API 연동
3. **OAuth 구현**: 소셜 로그인 실제 구현
4. **활동 지원**: 라운지 활동 지원 기능 완성

