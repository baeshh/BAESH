# 백엔드 테스트 환경 설정 가이드

## 1단계: 환경 변수 파일 생성

백엔드 디렉토리(`baesh-career-backend`)에 `.env` 파일을 생성하세요:

```bash
cd /Users/baesh/Desktop/BAESH-Career/baesh-career-backend
```

`.env` 파일 내용:

```env
# Server
PORT=3001
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# Database (SQLite 사용 - 개발용)
DATABASE_URL="file:./dev.db"

# JWT
JWT_SECRET=baesh-career-secret-key-change-in-production-2024
JWT_EXPIRES_IN=7d

# OpenAI / Upstage
UPSTAGE_API_KEY=up_TFwA4lkj0aVJx9QPLm6RIbc6t8MvT
UPSTAGE_BASE_URL=https://api.upstage.ai/v1
```

## 2단계: Prisma 마이그레이션 실행

```bash
cd /Users/baesh/Desktop/BAESH-Career/baesh-career-backend
npm run prisma:migrate
```

## 3단계: 백엔드 서버 실행

터미널 1번 (백엔드):

```bash
cd /Users/baesh/Desktop/BAESH-Career/baesh-career-backend
npm run dev
```

서버가 `http://localhost:3001`에서 실행됩니다.

## 4단계: 프론트엔드 실행

터미널 2번 (프론트엔드):

```bash
cd /Users/baesh/Desktop/BAESH-Career/baesh-career
npm run dev
```

프론트엔드가 `http://localhost:5173`에서 실행됩니다.

## 5단계: 테스트

1. 브라우저에서 `http://localhost:5173` 접속
2. 회원가입 또는 로그인
3. 기능 테스트:
   - 프로필 수정
   - AI 클론과 대화 (대화 내용이 DB에 저장됨)
   - 네트워킹 게시물 작성
   - 라운지에서 채용 공고 지원

## 데이터베이스 확인

Prisma Studio로 데이터베이스 내용 확인:

```bash
cd /Users/baesh/Desktop/BAESH-Career/baesh-career-backend
npm run prisma:studio
```

브라우저에서 `http://localhost:5555` 접속하여 데이터 확인 가능

## 문제 해결

### 포트 충돌
- 백엔드 포트 3001이 사용 중이면 `.env`에서 `PORT=3002`로 변경
- 프론트엔드의 `src/utils/api.ts`에서도 포트 변경 필요

### 데이터베이스 오류
```bash
# 데이터베이스 초기화
rm dev.db dev.db-journal
npm run prisma:migrate
```

### 의존성 오류
```bash
rm -rf node_modules package-lock.json
npm install
```

