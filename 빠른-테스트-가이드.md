# 🚀 빠른 테스트 가이드

## ✅ 이미 완료된 것
- ✅ `.env` 파일 생성 완료
- ✅ 의존성 설치 완료 (`npm install`)
- ✅ Prisma 클라이언트 생성 완료
- ✅ 데이터베이스 마이그레이션 완료 (`dev.db` 생성됨)

## 📝 테스트 방법

### 1단계: 백엔드 서버 실행

터미널 1번에서:

```bash
cd /Users/baesh/Desktop/BAESH-Career/baesh-career-backend
npm run dev
```

서버가 `http://localhost:3001`에서 실행됩니다.

**참고**: TypeScript 오류가 있어도 서버는 실행됩니다. 오류는 나중에 수정하면 됩니다.

### 2단계: 프론트엔드 실행

터미널 2번에서:

```bash
cd /Users/baesh/Desktop/BAESH-Career/baesh-career
npm run dev
```

프론트엔드가 `http://localhost:5173`에서 실행됩니다.

### 3단계: 브라우저에서 테스트

1. `http://localhost:5173` 접속
2. 회원가입 또는 로그인
3. 기능 테스트:
   - ✅ 프로필 수정 → 백엔드에 저장됨
   - ✅ AI 클론과 대화 → 대화 내용이 DB에 저장됨
   - ✅ 네트워킹 게시물 작성 → DB에 저장됨
   - ✅ 라운지에서 채용 공고 지원 → DB에 저장됨

## 🔍 데이터베이스 확인

다른 터미널에서:

```bash
cd /Users/baesh/Desktop/BAESH-Career/baesh-career-backend
npm run prisma:studio
```

브라우저에서 `http://localhost:5555` 접속하여 저장된 데이터 확인 가능

## ⚠️ TypeScript 오류 해결

현재 TypeScript 오류가 있지만, 서버는 실행됩니다. 나중에 수정하면 됩니다.

빠른 해결: `tsconfig.json`에서 strict 모드 완화 (이미 완료됨)

## 🎯 테스트 체크리스트

- [ ] 백엔드 서버가 `http://localhost:3001`에서 실행됨
- [ ] 프론트엔드가 `http://localhost:5173`에서 실행됨
- [ ] 회원가입 성공
- [ ] 로그인 성공
- [ ] 프로필 수정 후 저장 확인
- [ ] AI 클론 대화 후 DB에 저장 확인
- [ ] 네트워킹 게시물 작성 후 DB에 저장 확인
- [ ] 채용 공고 지원 후 DB에 저장 확인

